mapscripts RevelationMap_Gym4_MapScripts {}

script RevelationMap_Gym4_LeaderJaison { // Fairy-type Gym
    lock
    faceplayer
    if (var(VAR_GYM_DEFEATED_COUNTER) == 8) {
        msgbox("Jaison: Go to Challengers' Arena text", MSGBOX_NPC)
    } elif (flag(FLAG_DEFEATED_JAISON) == true) {
        msgbox("Jaison: Defeated text!", MSGBOX_NPC)
    } else {
        msgbox("Jaison: Before battle text!", MSGBOX_DEFAULT)
        trainerbattle_no_intro(TRAINER_JAISON, "Defeat text…")
        playfanfare(MUS_OBTAIN_BADGE)
        message("{PLAYER} beat Jaison!")
        waitfanfare
        waitmessage
        msgbox("Jaison: After battle text.", MSGBOX_AUTOCLOSE)
        setflag(FLAG_DEFEATED_JAISON)
        setvar(VAR_DEFEATED_JAISON, 1)
        addvar(VAR_GYM_DEFEATED_COUNTER, 1)
    }
    release
    end
}

script RevelationMap_Gym4_SchoolKidDara { // Fairy-type Trainer
	trainerbattle_single(TRAINER_DARA, "Dara: Before battle text!", "Defeat text…")
	msgbox("Dara: After battle text.", MSGBOX_AUTOCLOSE)
	end
}
